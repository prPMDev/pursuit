{
  "name": "pursuit-dashboard",
  "version": "0.1.0",
  "description": "Pursuit job search dashboard — local, self-hosted, anti-mass-apply",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "login": "node -e \"import('./browser.js').then(m => m.setupLogin())\""
  },
  "dependencies": {
    "dotenv": "^16.4.0",
    "express": "^4.18.0",
    "puppeteer-core": "^23.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "@picocss/pico": "^2.1.1",
    "@shoelace-style/shoelace": "^2.20.1",
    "tabulator-tables": "^6.3.1"
  }
}
